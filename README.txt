Instructions:
1. Install Python and Streamlit if not installed:
   pip install streamlit

2. Run the app:
   streamlit run app.py

3. You can modify 'soil_data.csv' and 'crop_calendar.csv' for your district-specific logic.

4. Deploy via https://streamlit.io/cloud using this folder.
